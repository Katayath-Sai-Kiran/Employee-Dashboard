import 'package:flutter/material.dart';

class PlaySlipScreen extends StatelessWidget {
  const PlaySlipScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}